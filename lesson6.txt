Прошу прощения за мой русский)
1. Проанализируйте задания предыдущих уроков.
a. В каких случаях необходима была явная передача указателя в качестве входных параметров и 
возвращаемых результатов или в качестве приёмника в методах?

Вэтих случаях "в качестве входных параметров и возвращаемых результатов".
Чтобы было удобно и потратить мало место в памяти, мы можем использовать указателей. "&" Этот символ
содержит адрес памяти, а * этим символом можно разыменовать.  


b. В каких случаях мы фактически имеем дело с указателями при передаче параметров, хотя явно их не указываем?
При передаче в структур и методах можно использовать.

2. Для арифметического умножения и разыменования указателей в Go используется один и тот же символ — оператор (*). 
Как вы думаете, как компилятор Go понимает, в каких случаях в выражении имеется в виду умножение,
 а в каких — разыменование указателя?
как вы говорили
 * часть типа ,
 * оператор(арифметического умножения)
оператору нужно два аргумента,то есть два значение. Часть типа это для разыменования.